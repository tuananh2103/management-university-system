export const environment = {
  production: true,
  // TODO: Thay bằng URL THẬT của service backend trên Railway, rồi push lại.
  // Lấy ở: Railway -> service backend -> Settings -> Networking -> Public Domain.
  // Nhớ giữ phần "/api" ở cuối. Ví dụ: https://backend-production-1a2b.up.railway.app/api
  apiUrl: 'https://REPLACE-WITH-RAILWAY-BACKEND-URL/api',
};
